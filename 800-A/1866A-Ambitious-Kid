#include<bits/stdc++.h>
#include<algorithm>
using namespace std;

int main(){
    int n;
    cin>>n;
    vector<int>a(n);
    int ans=INT_MAX;
    for(int i=0;i<n;i++) {
        cin>>a[i];
        if(a[i]<0) a[i]=-1*a[i];
        ans=min(ans,a[i]);
    }
    cout<<ans<<"\n";
    return 0;
}