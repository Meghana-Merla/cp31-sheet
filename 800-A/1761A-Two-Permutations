#include<bits/stdc++.h>
#include<algorithm>
using namespace std;

int main(){
    int t;
    cin>>t;
    while(t--){
        int a,b,n;
        cin>>n>>a>>b;
        if(a+b+2<=n) cout<<"Yes \n";
        else if(a==b&&a==n) cout<<"Yes \n";
        else cout<<"No \n";
    }
    return 0;
}